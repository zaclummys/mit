{
    "private": true,
    "scripts": {
        "server:start": "node server.js",
        "client:watch": "parcel watch ./client/index.html -d ./dist/client",
        "client:build": "parcel build ./client/index.html -d ./dist/client --no-source-maps --no-autoinstall",
        "build": "npm run client:build"

    },
    "engines": {
        "node": "^13.0.0"
    },
    "dependencies": {
        "compression": "^1.7.4",
        "express": "^4.17.1",
        "socket.io": "^2.3.0",
        "uuid": "^7.0.0"
    },
    "devDependencies": {
        "autoprefixer": "^8.0.0",
        "normalize.css": "^8.0.1",
        "parcel": "^1.12.4",
        "postcss-modules": "^3.2.0",
        "react": "^16.13.1",
        "react-dom": "^16.13.1",
        "socket.io-client": "^2.3.0"
    }
}
